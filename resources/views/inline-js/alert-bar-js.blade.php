{{-- <script type="text/javascript" id="alert-bar-js">
    function getCookie(name) {
        const value = `; ${document.cookie}`;
        const parts = value.split(`; ${name}=`);
        if (parts.length === 2) return parts.pop().split(';').shift();
    }

    const alertBanner = document.querySelector('.alert-bar');

    if (alertBanner !== undefined && alertBanner !== null) {

        const alertBannerCookie = getCookie(alertBanner.id);

        if (typeof alertBannerCookie === 'undefined') {
            alertBanner.classList.remove('d-none');
        }
    }
</script> --}}
